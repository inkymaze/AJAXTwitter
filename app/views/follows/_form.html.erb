

<% follow_state = current_user.follows?(user) ? "followed" : "unfollowed" %>


<button type="button" class="follow-toggle" data-user-id="<%= user.id %>" data-initial-follow-state="<%= follow_state %>"></button>


<!-- Notes: confused on the how the user.id is pulled from the frontend folder.
  I'm guessing it is accessible because it is an instance variable and the view can access them anywhere?
  -->

<!--  -->
